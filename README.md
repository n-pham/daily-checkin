**Daily Checkin** in AWS Lambda, aiming to use `durable` feature to store checkin dates in checkpoints.

**Cloud**

Create Lambda function beforehand with Handler: src/index.handler



**Local** 

```bash
npm install
node utils/localServer.js
```